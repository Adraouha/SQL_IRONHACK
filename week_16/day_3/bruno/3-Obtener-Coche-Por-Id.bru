meta {
  name: 3. Obtener Coche Por ID (GET)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/coches/{{cocheId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: isDefined
  res.body.marca: isDefined
  res.body.matricula: isDefined
}
